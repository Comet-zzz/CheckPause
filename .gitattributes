# Shell scripts and service configs are read by bash, systemd and nginx on
# Linux, where CRLF line endings break them. Pin the line endings so a
# checkout on any machine produces LF.
*.sh      text eol=lf
*.service text eol=lf
*.conf    text eol=lf
env.example text eol=lf
